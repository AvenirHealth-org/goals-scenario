# R scripts

This dir contains a couple of reference R scripts that might be useful

1. generate_scenarios.R - a modified version of the script used by Lauren to write out the scenario_definitions.json
2. benchmark.R - a script which reads in output parquet files in R, does a couple of simple calculations and benchmarks executions. Can be used for examples of how to read and use the output
